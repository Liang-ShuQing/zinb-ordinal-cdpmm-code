# `fig:graphical_model`

Graphical model of the longitudinal ZINB–cumulative logit joint mixed-effects model with CDPMM random effects.

## Files

- `document.tex` — TikZ/LaTeX source

## Run

```bash
latexmk -pdf document.tex
# or: pdflatex document.tex
```

Copy the resulting PDF into the manuscript as `graphical_model.pdf`.
